/**
 * Provides a replacement for the native Filter stack in web.xml, which only
 * allows a few basic rules for URL matching.
 * <p>
 * The main class of this package is {@link ConfigurableFilterStack}, which
 * is the top level Filter to place in web.xml replacing all the other filters.
 */
package uk.ac.warwick.util.web.filter.stack;